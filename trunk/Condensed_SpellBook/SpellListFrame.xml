<Ui xmlns="http://www.blizzard.com/wow/ui/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.blizzard.com/wow/ui/
..\FrameXML\UI.xsd">
	<Script file="SpellListFrame.lua" />
	<Button name="CSB_SpellListButtonTemplate" inherits="SecureActionButtonTemplate" virtual="true">
		<Size>
			<AbsDimension x="30" y="30" />
		</Size>
		<Layers>
			<Layer level="BORDER">
				<Texture name="$parentIconTexture" setAllPoints="true" hidden="true" />
				<FontString name="$parentSpellRank" inherits="NumberFontNormal" maxLines="1" hidden="true" justifyH="RIGHT">
					<Size>
						<AbsDimension x="20" y="20" />
					</Size>
					<Anchors>
						<Anchor point="BOTTOMRIGHT" relativePoint="BOTTOMRIGHT">
							<Offset>
								<AbsDimension x="0" y="0" />
							</Offset>
						</Anchor>
					</Anchors>
				</FontString>
			</Layer>
		</Layers>
		<Frames>
			<Cooldown name="$parentCooldown" inherits="CooldownFrameTemplate" />
		</Frames>
		<Scripts>
			<OnLoad>
				CSB_SpellListButton_OnLoad(self);
			</OnLoad>
			<OnEvent>
				CSB_SpellListButton_OnEvent(self,event);
			</OnEvent>
			<PostClick>
				CSB_SpellListButton_PostClick(self,arg1);
			</PostClick>
			<OnShow>
				CSB_SpellListButton_OnShow(self);
			</OnShow>
			<OnHide>
				CSB_SpellListButton_OnHide(self);
			</OnHide>
			<OnDragStart>
				CSB_SpellListButton_OnDrag(self);
			</OnDragStart>
			<OnReceiveDrag>
				CSB_SpellListButton_OnDrag(self);
			</OnReceiveDrag>
			<OnEnter>
				CSB_SpellLister:Show();
				CSB_SpellListButton_OnEnter(self);
			</OnEnter>
			<OnLeave>
				CSB_SpellListButton_OnLeave(self);
			</OnLeave>
		</Scripts>
		<NormalTexture name="$parentNormalTexture" file="Interface\Buttons\UI-Quickslot2">
			<Size>
				<AbsDimension x="48" y="48" />
			</Size>
			<Anchors>
				<Anchor point="CENTER">
					<Offset>
						<AbsDimension x="0" y="0" />
					</Offset>
				</Anchor>
			</Anchors>
		</NormalTexture>
		<PushedTexture file="Interface\Buttons\UI-Quickslot-Depress" />
		<HighlightTexture name="$parentHighlight" file="Interface\Buttons\ButtonHilight-Square" alphaMode="ADD" />
	</Button>
	
	<Frame name="CSB_SpellLister" enableMouse="true" hidden="true" parent="CSB_SpellBookFrame">
	<!--
		This frame will remain hidden until the mouse hovers over a spell in the Spellbook.
		Once that occurs, this frame will appear next to that spell with all available ranks.
		If the spell does not have more than 1 rank, then this frame stays hidden.
		
		The Size is flexible and set when it makes a list.
		The Anchor is flexible and set when it makes a list.
		The Layers are blank, only showing the buttons.
		The Buttons are not made until the list appears.
	!-->
		<Size>
			<AbsDimension x="1" y="1" />
		</Size>
		<Frames>

		</Frames>
		<Scripts>
			<OnLoad>
				CSB_SpellLister_OnLoad(self);
			</OnLoad>
			<OnLeave>
				self:Hide();
			</OnLeave>
		</Scripts>
	</Frame>
</Ui>
